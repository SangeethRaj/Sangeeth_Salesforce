// 1. Create an empty array named 'highPriorityLeads'.
// 2. Create a new lead object.
// 3. Use the '.push()' method to add that lead to the array.
// 4. Print the length of the array using '.length'.

const highPriorityLeads = [];

const myLead = 
  {
    firstName: 'Sangeeth',
    lastName: 'Raj',
    company: 'S Tech'
  }

highPriorityLeads.push(myLead);

console.log(highPriorityLeads.length);
